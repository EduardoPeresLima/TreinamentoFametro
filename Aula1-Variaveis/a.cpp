#include<bits/stdc++.h>

using namespace std;

int main(){
    // Declarando Variáveis
    int numero_inteiro;         // Variável do tipo inteiro (Positivos, Negativos, Zero)
    bool afirmacao;             // Variavel booleana (Verdadeiro / Falso) (True / False)
    char caractere;             // UM caractere (a, z, 8, ;, [, ])
    double numero_quebrado;     // 2.4, 9.8
    int primeiro_numero, segundo_numero, terceiro_numero; // Declarando várias variáveis de uma vez

    // Atribuindo valor a uma variável
    numero_inteiro = -50;       // Variável 'numero_inteiro' recebe valor 50
    afirmacao = true;           // Verdadeiro / 1
    afirmacao = false;          // Falso / 0
    caractere = '[';            // UM caractere, entre aspas SIMPLES
    numero_quebrado = 59.367;   // A casa decimal é separada por PONTO, NÃO VÍRGULA

    // Declarando e atribuindo um valor a uma variável

    char primeiro_caractere = '5', segundo_caractere = '+';     // Declarando e atribuindo dois caracteres
    double primeiro_double = 4.892, segundo_double = 4210.263;  // Declarando e atribuindo dois doubles
}