#include<bits/stdc++.h>

using namespace std;

int main(){
    // Vamos fazer um programa que receba dois numeros quebrados e mostre a media deles
    double numero_um, numero_dois;
    cout << "Insira o primeiro numero: ";
    cin >> numero_um;
    cout << "Insira o segundo numero: ";
    cin >> numero_dois;

    cout << "A media dos numeros eh " << (numero_um + numero_dois) / 2 << endl;
}